version: 2
jobs:
  hello:
    docker:
      - image: tkosht/webapi:latest
    steps:
      - checkout
      - run: cd backend && python -m pytest test

workflows:
  version: 2
  workflow:
    jobs:
    - hello
